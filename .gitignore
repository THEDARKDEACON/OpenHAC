# =============================================================================
# Python artifacts
# =============================================================================
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
.eggs/
*.egg
.venv/
venv/
env/

# =============================================================================
# KiCad generated outputs
# =============================================================================
*.net
*.csv
*.kicad_pcb
*.kicad_sch
*.kicad_pro
*.openhac-manifest.json
*.openhac-manifest.json.sha256
# OpenHaC secondary handoff artifacts (JSON/MD) and reports
*.openhac-*.json
*.openhac-*.md
*.openhac-*.txt
# KiCad CLI schematic ERC (SCH-003) and default *.rpt naming
*.kicad_sch.erc.txt
*-erc.rpt
*.kicad_sch.erc.json
# KiCad CLI PCB DRC reports (fabrication gate)
*.kicad_pcb.drc.txt
*.kicad_prl
# KiCad / editor lock files (e.g. ~project.kicad_pcb.lck)
*.lck
*.erc
_autosave-*
fp-info-cache
# Local KiCad library tables (project/user config; do not commit)
fp-lib-table
sym-lib-table
# IPC-2581 (kicad-cli pcb export ipc2581, MFG-001)
*.ipc2581
# KiCad 6+ project backups folder (if created locally)
*-backups/
*-backups-*/
# Optional local convention: custom DB path or scratch (see README / OPENHAC_DB_PATH)
.openhac/
openhac_local.db
# OpenHaC compile --zip-release default naming
*-release.zip
# Fab export / other build zips (when writing into repo tree)
*.zip

# =============================================================================
# SPICE outputs
# =============================================================================
*.cir
*.cir.ngspice.log

# =============================================================================
# Log files
# =============================================================================
*.log
report.txt
z3_out.log
physics.log
sync.log

# =============================================================================
# Database (generated by sync_catalog)
# =============================================================================
openhac/database/openhac.db
openhac/database/.cache/

# =============================================================================
# SKiDL runtime
# =============================================================================
.skidl/

# =============================================================================
# Generated build scripts (integration test outputs, not the tests/ suite)
# =============================================================================
test_complex.py
test_complex_sklib.py
test_massive.py
test_massive_sklib.py
test_simulate.py
test_simulate_sklib.py
build_sklib.py

# =============================================================================
# IDE / OS
# =============================================================================
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# =============================================================================
# Kiro spec configs (local workflow state)
# =============================================================================
.kiro/


# =============================================================================
# Pytest / coverage / test tooling outputs
# =============================================================================
.pytest_cache/
.coverage
.coverage.*
coverage.xml
htmlcov/
.tox/
.nox/
.pytest_tmp/
junit.xml
*.cover
*.py,cover
pytestdebug.log
.pytest_exec/
*.pytest_session.sqlite

# =============================================================================
# Tests — generated / local only (KEEP tests/**/*.py TRACKED for CI)
# =============================================================================
tests/__pycache__/
tests/**/__pycache__/
tests/.hypothesis/
tests/.hypothesis_tmp/
tests/.benchmarks/
tests/tmp/
tests/**/tmp/
tests/**/.tmp/
tests/.pytest_cache/
tests/.coverage
tests/htmlcov/
tests/junit.xml
# Optional local fixtures you generate (do not commit)
tests/fixtures/generated/

# =============================================================================
# Hypothesis / benchmarks (repo root)
# =============================================================================
.hypothesis/
.benchmarks/
prof/
*.prof

# =============================================================================
# mypy / ruff caches
# =============================================================================
.mypy_cache/
.ruff_cache/

# =============================================================================
# Jupyter / notebooks checkpoints
# =============================================================================
.ipynb_checkpoints/
*.ipynb_checkpoints

# =============================================================================
# Editor / merge backup files
# =============================================================================
*~
.#*
\#*\#
*.orig
*.bak
*.tmp
*.temp

# =============================================================================
# OS junk (extra patterns)
# =============================================================================
Desktop.ini
ehthumbs.db

# =============================================================================
# Compiled components file (large, regenerated)
# =============================================================================
components.txt

# =============================================================================
# DSN/SES autorouter intermediate files
# =============================================================================
*.dsn
*.ses

# =============================================================================
# Policy: what stays in git
# =============================================================================
# - Track: tests/**/*.py, tests/conftest.py (CI requires them).
# - Ignore: everything above under tests/ that is cache, Hypothesis, tmp, or coverage.
# - Do NOT add a blanket "tests/*.py" rule — that would break CI and code review.
# =============================================================================
