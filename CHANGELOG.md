# Changelog

Notable changes to OpenHaC. The package version in `pyproject.toml` is canonical; this file is checked in CI (SW-004).

## [0.2.0]

- Alpha development track: compiler manifest, BOM handoffs, KiCad integration, and production-readiness slices per `docs/PRODUCTION_READINESS_SPEC.md`.
